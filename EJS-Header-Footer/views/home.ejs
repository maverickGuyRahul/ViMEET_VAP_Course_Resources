<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <header>This is nav bar</header>
    <h1>Welcome</h1>

    <% if (locals.name) { %>

    <h1><%= locals.name %></h1>

    <% } %> 
    
    <% if (locals.surname) { %>

    <h1><%= locals.surname %></h1>

    <% } %>

    <footer>This is footer</footer>
  </body>
</html>
